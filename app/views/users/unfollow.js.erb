<% if @success %>
$(".<%= escape_javascript "#{@user._id}-follow" %>").html("<%= escape_javascript link_to('关注', follow_user_path(@user), remote: true, class: "btn btn-small btn-success") %>");
<% else %>
alert("取消关注失败！");
<% end %>