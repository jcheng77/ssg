var panel = $("p.tags_list");
$("#tag", panel).val("");
$("a.blingy").remove();

<% @item.tags_each do |t| %>
panel.prepend('<%= escape_javascript raw("<a href=\"#\" class=\"blingy\">##{t}<span class=\"left\"></span><span class=\"right\"></span></a>") %>');
<% end %>